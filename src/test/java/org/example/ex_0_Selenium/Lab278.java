package org.example.ex_0_Selenium;

import org.openqa.selenium.edge.EdgeDriver;
import org.testng.annotations.Test;

public class Lab278 {
@Test
    public void testvwoLogin(){
        // Selenium Flow
        EdgeDriver driver = new EdgeDriver();
        driver.get("https://sdet.live");
        driver.quit();
        // Selenium Flow
    //1. Create Session ID = q0982342355233525we3344swe
    // Client - Java (Program)
    //Server - Webdriver (edge) - Command W3C --> Edge Browser
    //2. Session - Run the commands - GET --> GET Method w3c Protocol
    //3.EdgeDriver(Server) ->Tell the Edge Browser and  open the URL - https://sdet.live
    //4. Open a Frsh copy of the Edge Browser and open the URL - https://sdet.live
    //5.Driver - Command Request (driver.quit())  - shutdown the browser. ID = null



    }
}
